include "std"
class Op
  id:int
  type:int
  token:ptr
  func:ptr

  method init id:int type:int token:ptr func:ptr -> ptr :
    Op.size malloc
    take op in

    id    op Op->id
    type  op Op->type
    token op Op->token
    func  op Op->func
    op
  end
endclass
